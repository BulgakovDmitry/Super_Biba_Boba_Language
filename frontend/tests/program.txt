fst = 0; // тип не требуется, все типы int
snd = 1;
iters = ?; // считать со stdin число, определить переменную
while (iters > 1) { // синтаксис для if такой же
    tmp = fst;
    fst = snd;
    snd = snd + tmp;
    print snd;
    iters = iters - 1;
}
print snd;
